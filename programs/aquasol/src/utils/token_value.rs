use anchor_lang::prelude::*;

// pub fn calculate_yt_token_value(yt_token_amount: u64, maturity_ts: i64, expected_apy: u64) -> u64 {
    // let now = Clock::get()?.unix_timestamp;
    // let time_remaining = maturity_ts - now;
    // let total_tokens = yt_token_amount * expected_apy;
    // let yt_token_value = total_tokens * (time_remaining / 86400);
    // return yt_token_value;
// }

// not complete